package com.javastreamThoery;

public class TheoryStreams {
// first we need to have objects in our collections and to process the object 
// in the collection we need to add it in stream, once the objects are there
//	in the stream then we can process this object by using 
// *** Filter(), map(), flatMap(), distinct(), limit()
// this are non terminal method once non terminal actions are completed 
// non terminal methods always returns a stream of object 
	
//	we can apply terminal operation like
//	collect(), reduce(), count(), min(), max(), foreach(), toArray()
	
}
// non Terminal methods => Filter(),map(),FlatMap(),distinct(),limit()
// Terminal Methods => collect(),reduce(),count(), min(), max(), foreach(), toArray()